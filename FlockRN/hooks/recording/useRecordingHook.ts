import useRecording from '@/hooks/recording/useRecording';

/**
 * Direct hook for recording functionality
 * Use this when you don't need global state management
 */
export const useRecordingHook = useRecording;

export default useRecordingHook;
